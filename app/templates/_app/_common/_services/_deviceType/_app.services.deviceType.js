'use strict';

/**
 * Register services for <%= device_type %> devices to this
 * module.
 */
angular.module('app.services.<%= device_type %>', [
    'app.services.common',
]);

/*

Example:

angular.module('app.services.<%= device_type %>').service('myService', function() {
    // some constructor
});
*/
