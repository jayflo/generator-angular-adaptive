'use strict';

/**
 * Register device independent services with this module.
 *
 * NOTES:
 * 1) Every device specific, module of services inherits this
 *     module.
 * 2) Put device independent services in
 *         /app/common/services
 */
angular.module('app.services.common', []);

/*

Example:

angular.module('app.services.common').service('myService', function() {
    // some constructor
});
*/
